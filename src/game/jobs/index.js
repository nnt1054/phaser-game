import TempJob from './temp';
import HealerJob from './healer';
import MeleeJob from './melee';

const jobMap = {
    'TMP': TempJob,
    'HEAL': HealerJob,
    'MELEE': MeleeJob,
}

export default jobMap;
